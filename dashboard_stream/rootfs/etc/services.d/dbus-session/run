#!/command/with-contenv bashio
# shellcheck shell=bash
set -e

bashio::log.info "Starting D-Bus session bus"
exec dbus-daemon \
    --config-file=/etc/dbus-1/kiosk-session.conf \
    --nofork \
    --nopidfile
